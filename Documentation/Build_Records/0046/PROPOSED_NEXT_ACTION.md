# Proposed Next Action

Run the bundled real Windows PowerShell 5.1 regression. Import only after the exact PASS endpoint. After Actions green, authorise Build 0047 for clinician review work-queue integration and governed export baseline.
