# Build 0058 rollback runbook

Use the transactional backup path returned by the importer and invoke the importer with `--rollback-backup`. Do not delete Git objects manually. Any pilot suspension or amendment rollback must preserve the original evidence and audit history.
