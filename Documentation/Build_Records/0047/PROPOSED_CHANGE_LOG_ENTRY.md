## 2026-07-20 - Build 0047

Added the Retatrutide controlled longitudinal dashboard, alert review workflow and clinician export baseline, including schemas, templates, examples, generator, validators, tests and transactional import controls.

## RC5 correction

Build 0047 preserves the Build 0045 longitudinal journey schema and any pre-existing shared ownership helper. Build 0047 uses build-specific paths and performs complete canonical path collision inventory during dry run before any apply transaction.

## RC6 correction

Correct the canonical Windows test invocation and add automatic post-apply, pre-commit rollback using the exact apply report and external transactional backup.
