
# CERTIAURA BUILD 0041 — READ FIRST

**Build title:** retatrutide evidence corpus citation graph and scientific review baseline
**Build ID:** CERT-BUILD-0041
**Authorisation:** GREEN_AUTHORISED by founder instruction dated 2026-07-20
**Package state:** GENERATED after package-level and synthetic validation; canonical implementation requires actual Project Genesis dry run, controlled apply, repository validation, commit, push and GitHub Actions green.
**Required repository checkpoint:** Build 0040 `ACTIONS_GREEN_CLOSED` at `6f4dfb11dfdc4b28fe736972864ec1acf1a1e056`.

## Purpose

Build 0041 converts the existing Retatrutide flagship and Build 0039 evidence-control architecture into a concrete, source-linked scientific baseline. It creates a primary-source corpus, claim-to-evidence matrix, citation graph, scientific review decision, living-evidence watch and machine validators.

## Safety and communication boundary

This package is an evidence-governance asset. It does not provide personal dosing, administration, reconstitution, procurement or treatment instructions. Retatrutide remains investigational and is not approved for public use.

## Controlled import sequence

1. Stop OneDrive before repository writes.
2. Pull `main` with fast-forward only and confirm HEAD `6f4dfb11dfdc4b28fe736972864ec1acf1a1e056` or a later explicitly authorised clean checkpoint.
3. Locate this ZIP by its SHA-256, not its filename.
4. Run the package preflight and actual installed Project Genesis dry run.
5. Review routing, conflicts, new UAI allocations, expected register total and continuity delta.
6. Run controlled apply with external backup root.
7. Run the Build 0041 validator and regression tests.
8. Confirm no runtime artefacts, unexpected deletions, unstaged changes or prior-build residue.
9. Stage all changes and run `git diff --check` and `git diff --cached --check`.
10. Commit with the exact locked message, push and confirm GitHub Actions green.
11. Record and verify the end-of-build lessons learned before closure.

## Exact commit message

`Add Certiaura Build 0041 retatrutide evidence corpus citation graph and scientific review baseline`

## One-link implementation bundle

Build 0041 package version 1.0.4 includes `Scripts/Run_Certiaura_Build_0041.ps1`. Delivery is through one implementation-bundle ZIP containing the importer-ready package, the PowerShell shortcut, its detached SHA-256 file and a thin double-click starter. The inner build package remains the only ZIP supplied to Project Genesis.

## Windows PowerShell 5.1 compatibility

All executable `.ps1` and `.cmd` files are ASCII-only. The implementation bundle starter must invoke the native PowerShell parser before executing the launcher. Any parser error blocks execution before repository access.

## One-click launcher correction - package v1.0.4

Run `START_BUILD_0041.cmd` from the extracted implementation-bundle folder. Do not copy or paste the contents of `Run_Certiaura_Build_0041.ps1` into an open PowerShell prompt. The launcher now resolves the bundled inner ZIP only from its own folder by exact SHA-256, passes that explicit path to the importer, and restarts OneDrive using an iterated full executable path rather than indexing a potentially scalar pipeline result.
