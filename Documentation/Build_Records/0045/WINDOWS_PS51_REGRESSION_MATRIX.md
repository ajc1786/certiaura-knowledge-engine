# Build 0045 Windows PowerShell 5.1 Regression Matrix

- Package hash verification.
- Flat repository-relative routing.
- ASCII-only PowerShell and native parser pass.
- Exact file classification and JSON/Python validation.
- Deliberate internal-backup duplicate register blocks import.
- Dry-run and transactional apply succeed after the negative fixture is removed.
- Three formal assets are allocated and reconciled.
- External backup is created outside the repository.
- Validator ignores unrelated UAI serial `000045` files.
- Seventeen automated tests pass.
- Journey, schedule and clinician handoff outputs are generated.
- Urgent and direct-identifier defective examples fail or route as designed.
- Runtime artefacts are absent.
- `git diff --check` and `git diff --cached --check` pass.

## Corrected raw seed append regression

- Seventeen automated tests pass.
- The packaged raw seed journey is supplied directly to the event-recorder command line.
- An urgent event appends without exception.
- All four raw seed events and the new urgent event receive deterministic event identifiers and event hashes.
- The resulting event count is five and the journey state is `LOCKED_URGENT_ROUTING`.
## Expected-negative native command handling

- The direct-identifier fixture must make the Python process return non-zero.
- Native stderr from this deliberate failure must not terminate the Windows PowerShell 5.1 harness before `$LASTEXITCODE` is captured.
- The original `$ErrorActionPreference` must be restored in `finally`.
- A rejected identifiable event must not create an output file.
- Any zero exit code, missing exit code or created output file blocks release.
